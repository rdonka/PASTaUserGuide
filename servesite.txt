# Use this file to serve the site to a local host - set directory to folder containing the docs and then serve.
# Copy the below code into Windows PowerShell.

cd  C:\Users\rmdon\Desktop\GitHub_MyRepositories\PASTaUserGuide
mkdocs serve

mkdocs gh-deploy --force